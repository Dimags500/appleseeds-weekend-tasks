// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No"
// string for false.

function yesOrNo(bool_value) {
  return bool_value ? "Yes" : "No";
}

const result = yesOrNo(false);
console.log(result);
